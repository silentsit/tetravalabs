{
  "name": "tetravalabs-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm install --include=dev && npm run build",
    "dev": "npm --prefix app run dev",
    "start": "npm --prefix app run start"
  },
  "dependencies": {
    "next": "^16.0.0"
  }
}
