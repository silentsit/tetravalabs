console.log(`
Tetrava Labs — deployment env mapping

Storefront (Vercel, root: apps/storefront)
  NEXT_PUBLIC_SITE_URL=https://tetravalabs.com
  NEXT_PUBLIC_MEDUSA_URL=https://<medusa-service>.onrender.com
  NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=<from Medusa Admin>
  REVALIDATE_SECRET=<random secret>
  RESTRICTED_COUNTRIES=AU,CA,...
  NEXT_PUBLIC_PLAUSIBLE_DOMAIN=tetravalabs.com
  TYPESENSE_PROTOCOL=https
  TYPESENSE_HOST=<typesense host>
  TYPESENSE_PORT=443
  TYPESENSE_API_KEY=<typesense key>
  TYPESENSE_COLLECTION=products
  RESEND_API_KEY=<resend key>
  RESEND_FROM=Tetrava Labs <orders@tetravalabs.com>
  SANITY_PROJECT_ID=<optional>
  SANITY_DATASET=production

Medusa (Render, render.yaml)
  DATABASE_URL=<Neon pooled connection string>
  USE_REDIS=true
  REDIS_URL=<Upstash or Render Key Value URL>
  JWT_SECRET=<auto-generated on Render>
  COOKIE_SECRET=<auto-generated on Render>
  STORE_CORS=https://tetravalabs.com,https://www.tetravalabs.com
  ADMIN_CORS=https://<medusa-service>.onrender.com
  AUTH_CORS=https://tetravalabs.com,https://www.tetravalabs.com
  CRYPTO_WEBHOOK_SECRET=<provider webhook secret>
  CRYPTO_CHECKOUT_BASE_URL=<BTCPay/NOWPayments checkout URL>
  RESEND_API_KEY=<resend key>

Post-deploy commands (Render preDeployCommand in render.yaml)
  npm run db:migrate --workspace=@tetrava/medusa
  npm run db:lab-schema --workspace=@tetrava/medusa

Manual one-time setup after Medusa deploy
  npm run catalog:import
  npm run typesense:index
  npm --prefix apps/medusa run coa:import-sample

Smoke checks
  npm run smoke:local
  SMOKE_STOREFRONT_URL=https://tetravalabs.com SMOKE_MEDUSA_URL=https://<medusa> npm run smoke:production
`)
